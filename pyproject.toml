[project]
name = "myrepo-actions-tdd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pre-commit>=4.6.0",
    "pytest>=9.0.3",
]
